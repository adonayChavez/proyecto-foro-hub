CREATE TABLE cursos ( id BIGSERIAL PRIMARY KEY, activo BOOLEAN NOT NULL, categoria VARCHAR(50) NOT NULL CHECK (categoria IN ('FRONTEND', 'BACKEND', 'DEVOPS', 'ROBOTICS', 'IA')), name VARCHAR(255) NOT NULL );