CREATE TABLE usuarios ( id BIGSERIAL PRIMARY KEY, apellido VARCHAR(255) NOT NULL, email VARCHAR(255) NOT NULL UNIQUE, enabled BOOLEAN NOT NULL, nombre VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, role VARCHAR(50) NOT NULL CHECK (role IN ('ADMINISTRADOR', 'USUARIO', 'EXPECTADOR')), username VARCHAR(255) NOT NULL UNIQUE );